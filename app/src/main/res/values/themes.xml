<resources>
    <!-- Compose draws the real UI; this theme only covers the window before Compose
         starts. Dark so there is no white flash on launch. -->
    <style name="Theme.DisasterMesh" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
